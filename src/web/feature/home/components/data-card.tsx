import { Card } from "antd";
import styled from "styled-components";

export const DataCard = styled(Card)`
  .ant-card-head {
    padding-left: 16px;
    padding-right: 16px;
    border-bottom: none;
  }
  .ant-card-body {
    padding: 0;
  }
`;
